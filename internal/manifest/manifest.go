// Package manifest tracks which files are managed by offstage for a given
// project (via the sync config) and for global state.
package manifest

// TODO(offstage-1v1): implement manifest / sync config parsing.
