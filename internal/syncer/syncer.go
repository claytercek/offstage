// Package syncer implements the push/pull logic between the local filesystem
// (target state) and the sync store (source state).
package syncer

// TODO(offstage-393): implement push/pull and git passthrough.
